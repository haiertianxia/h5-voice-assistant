# 🎙️ H5 语音助手 — 需求规格说明书（SRS）

> 版本：v1.1 | 日期：2026-05-14 | 状态：已更新（Mermaid 图表）

---

## 一、项目概述

### 1.1 项目背景

H5 语音助手是一款**移动端优先**的语音交互 Web 应用。用户长按圆形按钮说话，松手后 AI 自动理解并以语音+文字同步回复。核心目标是提供极低门槛的语音交互体验，无需打字，开口即用。

### 1.2 项目范围

| 范围 | 说明 |
|------|------|
| **目标平台** | 移动端浏览器（H5）、微信内嵌浏览器 |
| **部署方式** | Node.js 后端 + 纯前端静态资源，Nginx 反向代理 |
| **核心能力** | 语音录制 → 语音识别 → 大模型对话 → 语音合成 → 回复播放 |
| **适用场景** | 老年人/手部不便用户、无键盘输入场景、快速问答 |

### 1.3 成功标准

- 移动端 Safari / Chrome / 微信浏览器可完整运行
- 端到端延迟（录音结束 → 首次文字出现）< 3s（网络正常时）
- 语音识别准确率满足日常对话需求
- 无需安装 App，通过 URL 即可访问

---

## 二、用户故事与功能列表

### 2.1 用户故事

| 角色 | 故事 | 优先级 |
|------|------|--------|
| 普通用户 | 我想长按说话，不用打字就能获得 AI 回答 | P0 |
| 普通用户 | 我希望 AI 回答能自动朗读，不用看屏幕 | P0 |
| 普通用户 | 我想调节 AI 说话的音量和速度 | P1 |
| 普通用户 | 我在嘈杂环境中能清楚知道是否在录音 | P1 |
| 开发者 | 我希望 API 接口有调用频率限制，防止滥用 | P1 |
| 开发者 | 关键配置（API Key）不能在前端暴露 | P0 |
| 开发者 | 后端服务要有健康检查接口，方便监控 | P2 |

### 2.2 功能清单

**P0 — 核心功能**

- [F1] 语音录制 — 长按 orb 开始，松开自动停止
- [F2] 语音识别 — 优先浏览器 Web Speech API，备用水星 STT API
- [F3] LLM 对话 — 调用水星 ARK API 获取回复
- [F4] 语音合成 — 优先服务器 TTS（水星），备选浏览器 Web Speech
- [F5] 文字+语音同步 — AI 回复先打字，后自动播放音频
- [F6] 敏感信息保护 — API Key 不在前端代码中硬编码

**P1 — 交互体验**

- [F7] 录音状态视觉反馈 — orb 放大+变色+波形动画
- [F8] 音量/语速设置 — 滑块调节，localStorage 持久化
- [F9] 静音模式 — 一键静音，顶部图标切换
- [F10] 错误提示 — Toast 提示用户权限拒绝/网络错误/无声音
- [F11] 麦克风权限引导 — 首次拒绝后显示开启权限说明

**P2 — 后端运维**

- [F12] 请求频率限制 — 每 IP 每分钟最多 30 次调用
- [F13] 健康检查接口 — GET /api/health，返回各服务可用状态
- [F14] 音频文件自动清理 — 启动时删除 1 小时前的临时文件
- [F15] API 请求超时保护 — 防止外部服务卡死导致后端挂起

---

## 三、用例图（Use Case Diagram）

```mermaid
graph TD
    %% 参与者
    User([:User 普通用户])
    Developer([:Actor 开发者])

    %% 系统边界
    subgraph SYSTEM["H5 Voice Assistant 系统"]
        UC1[": UC1 发起语音对话"]
        UC2[": UC2 调整语音设置"]
        UC3[": UC3 运维监控"]
        UC4[": UC4 错误处理"]
    end

    %% 用例关系
    User --> UC1
    User --> UC2
    Developer --> UC3
    User -.-> UC4

    %% UC1 展开
    UC1 --- UC1a["长按 orb 开始录音"]
    UC1 --- UC1b["松手发送"]
    UC1 --- UC1c["等待 AI 回复"]
    UC1 --- UC1d["听 AI 语音回复"]
```

---

## 四、业务流程（Activity Diagram）

### 4.1 核心语音对话流程

```mermaid
flowchart TD
    START([开始]) --> IDLE{等待长按}
    IDLE --"长按 orb (120ms)"--> PERM{麦克风权限}
    PERM --"拒绝"--> HINT1["显示权限申请提示"]
    HINT1 --> END1([结束])
    PERM --"允许"--> RECORD[开始录音<br/>orb变色+波形动画]

    RECORD --> SILENCE{3秒无声音?}
    SILENCE --"是"--> AUTO_STOP[自动停止]
    SILENCE --"否"--> RELEASE{松手}
    RELEASE --"松手"--> STOP[停止录音]
    RELEASE --"超时60s"--> STOP

    STOP --> SMALL{音频<500B?}
    SMALL --"是"--> ERR1["Toast: 没检测到声音"]
    ERR1 --> END2([结束])
    SMALL --"否"--> STT[发送 STT API]

    STT --> STT_OK{识别成功?}
    STT_OK --"否"--> ERR2["Toast: 没听清楚"]
    ERR2 --> END3([结束])
    STT_OK --"是"--> SHOW_USER[显示用户消息气泡]

    SHOW_USER --> CHAT[发送 ARK API]
    CHAT --> THINKING["显示思考中..."]
    THINKING --> REPLY[获取 AI 回复]
    REPLY --> TYPE["打字机动画显示"]
    TYPE --> MUTED{静音模式?}
    MUTED --"是"--> END4([结束])
    MUTED --"否"--> TTS[播放 TTS 音频]
    TTS --> END4

    style START fill:#e1f5fe
    style END1 fill:#ffebee
    style END2 fill:#ffebee
    style END3 fill:#ffebee
    style END4 fill:#e8f5e9
```

### 4.2 设置调节流程

```mermaid
flowchart LR
    CLICK[点击设置按钮] --> PANEL[设置面板滑入]
    PANEL --> BRANCH{用户操作}
    BRANCH --> VOL[调节音量滑块]
    BRANCH --> SPEED[调节语速滑块]
    BRANCH --> TTS_SRC[切换 TTS 来源]
    BRANCH --> MUTE[静音开关 Toggle]
    VOL --> SAVE["localStorage['vaSettings']"]
    SPEED --> SAVE
    TTS_SRC --> SAVE
    MUTE --> SAVE
    SAVE --> APPLY["实时更新 player<br/>立即生效"]
    APPLY --> CLOSE[关闭面板或继续调节]
```

---

## 五、序列图（Sequence Diagram）

### 5.1 完整语音对话时序

```mermaid
sequenceDiagram
    autonumber
    actor User as 用户
    participant Browser as 浏览器前端
    participant Server as Node.js 后端
    participant STT as AssemblyAI
    participant LLM as ARK API
    participant TTS as ByteDance TTS

    User->>Browser: 长按 orb (120ms)
    Browser->>Browser: getUserMedia(音频)
    Note over Browser: 录音中：波形动画

    User->>Browser: 松手 / 超时60s
    Browser->>Browser: recorder.stop()
    Browser->>Server: POST /api/stt<br/>multipart: audio/webm
    Server->>STT: POST upload
    STT-->>Server: upload_url
    Server->>STT: POST /v2/transcript
    Note over Server,STT: polling 30次，每次1s
    loop polling
        Server->>STT: GET /transcript/{id}
        STT-->>Server: status
    end
    STT-->>Server: {text: "用户说的话"}
    Server-->>Browser: {text}
    Browser->>Browser: 显示用户消息气泡

    Browser->>Server: POST /api/chat<br/>{text, voiceSpeed}
    Server->>LLM: POST /chat/completions
    LLM-->>Server: {reply}
    Server->>TTS: POST /api/v1/tts
    TTS-->>Server: audio/mp3 buffer
    Server->>Server: 保存 MP3 文件
    Server-->>Browser: {reply, audioUrl}
    Browser->>Browser: 打字机动画显示文字
    alt audioUrl 存在
        Browser->>Browser: 播放音频 + 进度条
    else audioUrl 为 null
        Note over Browser: 使用浏览器 Web Speech
    end
    Browser-->>User: 语音播放完成
```

---

## 六、质量需求

### 6.1 性能指标

| 指标 | 目标值 | 说明 |
|------|--------|------|
| 页面首次加载 | < 2s | 静态资源 gzip 后 < 100KB |
| STT 延迟 | < 5s | AssemblyAI 标准转录 |
| ARK 响应 | < 3s | 包含网络延迟 + 模型推理 |
| TTS 生成 | < 2s | 短文本语音合成 |
| 打字机动画 | 每字 28ms | 约 35 字/秒 |

### 6.2 兼容性

| 平台 | 最低版本 |
|------|---------|
| Chrome | 66+ |
| Safari iOS | 14.5+ |
| Safari macOS | 14.1+ |
| 微信内置浏览器 | 基于 Chromium |

### 6.3 安全需求

- 全站 HTTPS（生产环境必须，微信要求）
- API Key 存储在后端 `.env`，前端完全不接触
- CORS 生产环境配置明确 origin
- 请求频率限制：30 req/min/IP
- 音频上传大小限制：10MB
