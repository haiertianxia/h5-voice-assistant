# 🎙️ H5 语音助手 — 技术选型与架构设计

> 版本：v1.1 | 日期：2026-05-14 | Mermaid 图表

---

## 一、技术栈概览

| 层级 | 技术 | 选型理由 |
|------|------|---------|
| 前端框架 | 原生 HTML/CSS/JS | 无需构建工具，体积小，加载快 |
| 样式 | CSS Variables + 原生 CSS | 主题色统一，无依赖 |
| 录音 | MediaRecorder API | 浏览器原生，无需 SDK |
| 语音识别 | Web Speech API → AssemblyAI | 优先浏览器免费，失败后服务端兜底 |
| 语音合成 | 水星 TTS → Web Speech API | 优先高质量服务端合成 |
| 可视化 | Canvas API | 波形绘制，无依赖 |
| 后端框架 | Express.js | 轻量，生态丰富 |
| 运行时 | Node.js 18+ | 原生 ES Module 支持 |
| HTTP 客户端 | node-fetch v3 | 兼容浏览器 fetch API |
| 音频上传 | Multer | 接收浏览器上传，不写磁盘直接转 |
| LLM | Volcengine ARK | 大模型对话 |
| TTS | ByteDance | 语音合成 |
| STT | AssemblyAI | 语音识别（服务端兜底）|

---

## 二、系统架构图（C4 模型）

### 2.1 Context（上下文视图）

```mermaid
C4Context
    Person(user, "用户", "使用手机浏览器与语音助手交互")
    System_Boundary(sa, "H5 Voice Assistant") {
        System(frontend, "Web 前端", "纯 HTML/CSS/JS，静态部署")
        System(backend, "Node.js 后端", "Express.js，处理 STT/TTS/Chat 请求")
        SystemDb(storage, "本地文件系统", "临时 MP3 文件，1h 自动清理")
    }
    System_Ext(ark, "Volcengine ARK", "LLM 大脑，对话生成")
    System_Ext(tts, "ByteDance TTS", "服务端语音合成")
    System_Ext(stt, "AssemblyAI", "服务端语音识别")
    System_Ext(ws, "Web Speech API", "浏览器内置语音识别/合成")

    Rel(user, frontend, "HTTPS", "麦克风/音频")
    Rel(frontend, backend, "HTTPS API", "JSON/multipart")
    Rel(backend, ark, "HTTPS", "对话请求")
    Rel(backend, tts, "HTTPS", "语音合成")
    Rel(backend, stt, "HTTPS", "语音识别")
    Rel(frontend, ws, "浏览器内部", "免费 STT/TTS")
    Rel(backend, storage, "写入/读取", "MP3 文件")
```

### 2.2 Container（容器视图）

```mermaid
C4Container
    Person(user, "用户", "手机浏览器")

    System_Boundary(va, "H5 Voice Assistant") {
        Container(spa, "单页应用", "HTML/CSS/JS", "静态资源，Nginx 服务")
        Container(be, "API 服务", "Node.js / Express", "端口 3000")
        ContainerDb(fs, "文件系统", "临时文件", "server/uploads/*.mp3")
    }

    System_Ext(ark, "ARK API", "对话生成")
    System_Ext(tts, "ByteDance TTS", "语音合成")
    System_Ext(stt, "AssemblyAI", "语音识别")

    Rel(user, spa, "HTTPS", "用户交互")
    Rel(spa, be, "HTTP", "/api/chat, /api/stt, /api/tts")
    Rel(be, ark, "HTTPS", "POST /chat/completions")
    Rel(be, tts, "HTTPS", "POST /api/v1/tts")
    Rel(be, stt, "HTTPS", "POST /v2/upload, /v2/transcript")
    Rel(be, fs, "R/W", "保存/读取 MP3")
```

---

## 三、部署架构图（Deployment Diagram）

```mermaid
deploymentDiagram
    actor user as 用户(手机)
    node nginx["Nginx\n(TLS 终止 / 反向代理)"]
    node nodejs["Node.js\nExpress\n端口 3000"]
    node fs["文件系统\nserver/uploads/"]
    cloud ark["Volcengine ARK API"]
    cloud tts["ByteDance TTS API"]
    cloud stt["AssemblyAI API"]
    cloud browser["Web Speech API\n(浏览器内置)"]

    user -->|"HTTPS :443"| nginx
    nginx -->|"HTTP :3000"| nodejs
    nodejs --> ark
    nodejs --> tts
    nodejs --> stt
    nodejs --> fs
    user -->|"浏览器内部"| browser
```

---

## 四、工程结构（C4 Component 视图）

```mermaid
C4Component
    Container_Boundary(frontend, "public/ (静态资源)") {
        Component(index, "index.html", "主页面", "HTML5")
        Component(style, "css/style.css", "样式", "CSS3 + Variables")
        Component(app, "js/app.js", "VoiceAssistant", "主控制器")
        Component(rec, "js/recorder.js", "AudioRecorder", "录音+波形")
        Component(player, "js/player.js", "AudioPlayer", "播放+进度")
        Component(ui, "js/ui.js", "UIController", "DOM 操作")
    }

    Container_Boundary(backend, "server/ (Node.js)") {
        Component(index, "index.js", "Express App", "路由注册/中间件")
        Component(ratelimit, "utils/ratelimit.js", "RateLimiter", "30 req/min/IP")
        Component(chat, "routes/chat.js", "Chat Router", "POST /api/chat")
        Component(tts, "routes/tts.js", "TTS Router", "POST /api/tts")
        Component(stt, "routes/stt.js", "STT Router", "POST /api/stt")
        Component(ark_svc, "services/ark.js", "ARK Service", "大模型调用")
        Component(tts_svc, "services/tts.js", "TTS Service", "语音合成")
    }

    Rel(app, rec, "uses")
    Rel(app, player, "uses")
    Rel(app, ui, "uses")
    Rel(index, ratelimit, "uses")
    Rel(index, chat, "mounts")
    Rel(index, tts, "mounts")
    Rel(index, stt, "mounts")
    Rel(chat, ark_svc, "calls")
    Rel(chat, tts_svc, "calls")
```

---

## 五、项目工程结构

```
h5-voice-assistant/
├── docs/                          # 设计文档 (Mermaid)
│   ├── 01_需求规格说明书.md
│   ├── 02_技术选型与架构设计.md     ← 本文档
│   ├── 03_概要设计.md
│   ├── 04_详细设计.md
│   └── 05_接口设计.md
│
├── server/                        # Node.js 后端
│   ├── index.js                   # Express 入口
│   ├── utils/
│   │   └── ratelimit.js          # 内存限流器
│   ├── routes/
│   │   ├── chat.js               # POST /api/chat
│   │   ├── tts.js                # POST /api/tts
│   │   └── stt.js                # POST /api/stt
│   └── services/
│       ├── ark.js                 # ARK API (15s 超时)
│       ├── tts.js                 # TTS API (10s 超时)
│       └── uploads/               # 临时 MP3 文件
│
├── public/                        # 静态资源
│   ├── index.html
│   ├── css/style.css
│   └── js/
│       ├── app.js                 # VoiceAssistant
│       ├── recorder.js             # AudioRecorder
│       ├── player.js              # AudioPlayer
│       ├── ui.js                  # UIController
│       └── stt.js                # SpeechRecognizer
│
├── .env.example
├── .gitignore
├── package.json
└── README.md
```

---

## 六、部署架构

### 开发环境

```bash
npm start
# 访问 http://localhost:3000
# 微信开发者工具：配置合法域名 http://localhost:3000
```

### 生产环境（Nginx）

```nginx
server {
    listen 443 ssl http2;
    server_name your-domain.com;

    ssl_certificate     /path/to/fullchain.pem;
    ssl_certificate_key /path/to/privkey.pem;
    ssl_protocols       TLSv1.2 TLSv1.3;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /uploads/ {
        alias /path/to/server/uploads/;
        add_header Cache-Control "public, max-age=3600";
    }
}
```

---

## 七、安全设计

```mermaid
flowchart LR
    subgraph 威胁["威胁模型"]
        A1["API Key 前端暴露"]
        A2["CORS 劫持"]
        A3["频率滥用"]
        A4["文件上传攻击"]
        A5["中间人"]
    end

    subgraph 应对["应对方案"]
        S1["Key 只在后端 .env，前端不接触"]
        S2["CORS 明确 origin，生产配置"]
        S3["30 req/min/IP 内存限流"]
        S4["Multer 强制 10MB + Content-Type 验证"]
        S5["TLS 1.2+，生产强制 HTTPS"]
    end

    A1 --> S1
    A2 --> S2
    A3 --> S3
    A4 --> S4
    A5 --> S5
```

---

## 八、运维与监控

```mermaid
flowchart LR
    subgraph 健康检查["GET /api/health"]
        H1["ark: true/false"]
        H2["tts: true/false"]
        H3["stt: true/false"]
        H4["time: ISO8601"]
    end

    subgraph 日志级别
        L1["error → API失败/超时/权限错误"]
        L2["warn → 降级/key未配置"]
        L3["log → 启动/生成成功"]
    end

    subgraph 启动检查
        C1["✓ .env 存在且 ARK_API_KEY 已填写"]
        C2["✓ HTTPS 证书已配置（生产）"]
        C3["✓ server/uploads/ 目录存在且可写"]
        C4["✓ 端口 3000 未被占用"]
    end
```
